name: Build Android APK

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      API_BASE_URL: ${{ secrets.API_BASE_URL }}
      API_AUTH_CODE: ${{ secrets.API_AUTH_CODE }}
      IMAGE_BASE_URL: ${{ secrets.IMAGE_BASE_URL }}
      BACKEND_URL: ${{ secrets.BACKEND_URL }}
      MOVIE_API_KEY: ${{ secrets.MOVIE_API_KEY }}
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
      APK_VERIFY_SECRET: ${{ secrets.APK_VERIFY_SECRET }}
      PLAYBACK_DEFAULT_REFERER: ${{ secrets.PLAYBACK_DEFAULT_REFERER }}
      PLAYBACK_DEFAULT_USER_AGENT: ${{ secrets.PLAYBACK_DEFAULT_USER_AGENT }}
      PLAYBACK_X_REQUEST_X: ${{ secrets.PLAYBACK_X_REQUEST_X }}
      PLAYBACK_ACCEPT_ENCODING: ${{ secrets.PLAYBACK_ACCEPT_ENCODING }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Generate environment file
        run: npm run generate-env

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Build Android Release APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon --refresh-dependencies

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release-unsigned
          path: android/app/build/outputs/apk/release/app-release.apk
